name: Build and Publish NuGet Package
on:
  push:
    tags: ['*']
    paths:
      - '.github/workflows/publish-package.yml'
      - 'src/AwsScheduleExpressionValidator/**'
  
  workflow_call:

  workflow_dispatch:
    inputs:
      tag:
        description: Existing tag to run against (e.g. v1.2.0). Leave empty to use selected ref.
        required: false
        type: string

jobs:
  pack-and-publish:
    permissions:
      contents: read
      id-token: write  # enable GitHub OIDC token issuance for this job

    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.tag != '' && format('refs/tags/{0}', github.event.inputs.tag) || github.ref }}
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: dotnet pack
        run: dotnet pack -c Release -o artifacts/package/release
        working-directory: src/AwsScheduleExpressionValidator

      # Use the ambient GitHub token to login to NuGet and retrieve an API key
      - name: NuGet login (OIDC → temp API key)
        uses: NuGet/login@v1
        id: login
        with:
          user: ${{ secrets.NUGET_USER }}

      - name: push to NuGet
        # Only push to NuGet if we're building a tag
        if: startsWith(github.ref, 'refs/tags/') || (github.event_name == 'workflow_dispatch' && github.event.inputs.tag != '')
        shell: bash
        # Loop through all the packages in the output folder and push them to
        # nuget.org, using the NUGET_API_KEY generated by the previous login step
        run: |
          for package in artifacts/package/release/*.nupkg; do
            dotnet nuget push "$package" \
              --api-key "${{ steps.login.outputs.NUGET_API_KEY }}" \
              --skip-duplicate \
              --source https://api.nuget.org/v3/index.json
          done
        working-directory: src/AwsScheduleExpressionValidator
